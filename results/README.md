# Saved results

`results.mat` is the MATLAB result structure produced by the original experiment. It contains picture metrics, video-sequence metrics, tracking metrics, category names, and resource summaries.

Because results depend on the dataset version, machine, MATLAB release, and configuration, rerun the pipeline before making a new quantitative claim. The public README describes the metric fields and the generated visual artifacts without presenting the saved run as a universal benchmark.
